{
  "name": "hr-automation",
  "version": "1.0.0",
  "private": true,
  "description": "HR hiring automation — Google Sheets + a Next.js dashboard, no separate backend.",
  "scripts": {
    "test": "node --test \"tests/**/*.test.js\"",
    "bootstrap:sheets": "node scripts/bootstrap-sheets.mjs",
    "check:sheets": "node scripts/bootstrap-sheets.mjs --check",
    "seed:demo": "node scripts/bootstrap-sheets.mjs --seed-demo",
    "verify": "npm run test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "googleapis": "^144.0.0"
  }
}
